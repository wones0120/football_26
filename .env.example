APP_ENV=development
APP_HOST=0.0.0.0
APP_PORT=8000

PGHOST=localhost
PGPORT=5432
PGDATABASE=football_26_dev
PGUSER=postgres
PGPASSWORD=postgres

# Optional override. If set, this takes precedence over PG* fields.
# DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/football_opt

# Dev convenience: create missing tables automatically on API startup.
AUTO_CREATE_TABLES=true
